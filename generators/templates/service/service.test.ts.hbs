import {{camelCase name}}Service from '@services/{{kebabCase name}}'

describe('{{pascalCase name}} service', () => {
  it('no errors', () => {
      expect({{camelCase name}}Service).toBeTruthy();
  });
})
