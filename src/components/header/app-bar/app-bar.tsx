import React, { FunctionComponent } from 'react';
import { AppBarProps } from './app-bar.props';
import { Wrapper } from './app-bar.styled';

const AppBar: FunctionComponent<AppBarProps> = ({ title }) => {
  return <Wrapper />;
};

export default AppBar;
